name: family_ledger_mobile
description: Family Ledger mobile client for iOS and Android.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0
  http: ^1.2.0
  http_parser: ^4.0.0
  flutter_secure_storage: ^10.0.0
  intl: ^0.20.0
  shared_preferences: ^2.2.0
  file_picker: ^11.0.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
